<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_avr_team_form" model="ir.ui.view">
        <field name="name">avr.team.form</field>
        <field name="model">avr.team</field>
        <field name="arch" type="xml">
            <form string="Repair Team">
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="%(action_avr_tasks)s" type="action"
                                class="oe_stat_button" icon="fa-tasks">
                            <field name="task_count" widget="statinfo" string="Tasks"/>
                        </button>
                    </div>
                    <group>
                        <field name="name"/>
                        <field name="team_type"/>
                        <field name="leader_id"/>
                    </group>
                    <group string="Team Members">
                        <field name="member_ids" widget="many2many_tags" nolabel="1"/>
                    </group>
                    <group string="Notes">
                        <field name="notes" nolabel="1"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_avr_team_list" model="ir.ui.view">
        <field name="name">avr.team.list</field>
        <field name="model">avr.team</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="team_type"/>
                <field name="leader_id"/>
                <field name="task_count"/>
            </list>
        </field>
    </record>

    <record id="action_avr_team" model="ir.actions.act_window">
        <field name="name">Teams</field>
        <field name="res_model">avr.team</field>
        <field name="view_mode">list,form</field>
    </record>

</odoo>
